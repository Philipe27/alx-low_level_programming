#include <stdio.h>
/**
 * main - This the entry point of tye function
 *
 * This function prints out the _putchar ffunction to console
 *
 * Return Always 0
 */
int main(void)
{
	char apl = 'a'
		while(apl <= 'z')
		{
			putchar(alp);
			apl++;
		}
	putchar('\n');
	return (0);
}
