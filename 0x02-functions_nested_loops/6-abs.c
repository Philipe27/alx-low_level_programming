#include "main.h"
/**
 * _abs - This is the start of the function
 * @n: Is a place holder
 *
 * This functin returns the absolute value of n
 *
 * Return: Always 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
